<!--components/area-header/area-header.wxml-->
<view class="header">
    <view class="title">{{ title }}</view>
    <view class="more" wx:if="{{ hasMore }}" bind:tap="clickMore">
        <text class="text">更多</text>
        <van-icon name="arrow" />
    </view>
</view>
